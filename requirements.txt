# Phase 1: CSD API (only installable in CCDC-licensed environment)
# ccdc                      # CSD Python API — installed via CCDC installer

# Phase 2: Paper download
scansci-pdf>=0.6.0
undetected-chromedriver>=3.5.0
selenium>=4.15.0

# Phase 3: LLM extraction
pandas>=2.2.0
openpyxl>=3.1.5
PyMuPDF>=1.24.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
loguru>=0.7.0

# Shared
python-dotenv>=1.0.0
